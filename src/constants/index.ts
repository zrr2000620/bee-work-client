export const DEFAULT_NAME = 'Umi Max';

export enum PayType {
  WECHAT = 'wxpay',
  ALI = 'aliay',
}

export enum UserStatus {
  UN_OPEN = 'Not',
  VALID = 'Valid',
  INVALID = 'Invalid',
}

export const STORAGE_TOKEN_KEY = 'token';
